<%= render layout: 'shards/default_box', locals: { box_title: "新建标签组", box_class: "box-primary", box_grid: "col-md-6" }  do |box|%>
<div class="col-md-12">
	 <% case box when Box::Body %>
	  <div class="well">
	    编辑标签组
	    <%= link_to '返回', system_tag_groups_path %>
	  </div>
	  <%= render 'form' if box.eql? Box::Body%>
	 <%end%>
</div>
<%end%>
